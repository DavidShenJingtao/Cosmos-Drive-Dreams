# Using TextGrad as prompt optimizer

## Quick start

Before running the `run_textgrad.py`, please execute the following commands:
1. move to `textgrad` directory
2. `conda activate cosmos-drive-dreams`
3. `export OPENAI_API_KEY=<your API key>`
4. `export PYTHONPATH="..:$PYTHONPATH"`
